/**
 * Theme JavaScript Imports
 * These files are imported here for Vite bundling and code splitting
 *
 * Note: Many theme scripts depend on jQuery and expect jQuery to be global.
 * These will be loaded dynamically or initialized in components that use them.
 */

// Import theme JS files - using dynamic imports for better code splitting
// Note: These files may depend on jQuery being available globally

// Initialize theme functionality
export function initTheme() {
    // This will be called after the app mounts
    // Individual theme scripts can be initialized here or in components
}
